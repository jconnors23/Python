 if len(s) == 1:
            return False
        stack = deque()
        for char in s:
            if char == '(' or char == '{' or char == '[':
                stack.append(char)
            elif char == ')' and stack[0] != '(':
                return False
            elif char == '}' and stack[0] != '{':
                return False
            elif char == ']' and stack[0] != ']':
                return False
            else:
                stack.pop()
        return len(stack) == 0